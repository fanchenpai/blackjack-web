<!DOCTYPE html>
<html>
  <head>
    <title>Blackjack Card Game</title>
    <link rel="stylesheet" href="/vendor/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/application.css">
  </head>
  <body>

    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="brand">Blackjack Game</a>
          <ul class="nav pull-left">
            <li>
              <a href="http://blooming-waters-1264.herokuapp.com/" target="_blank">Ajax Ver.</a>
            </li>
          </ul>
          <ul class="nav pull-right">
            <% if session.has_key?(:balance)%>
              <li>
                <a>Balance:<strong class="text-info"> <%= session[:balance] %></strong></a>
              </li>
            <% end %>
            <% if session.has_key?(:bet) && session[:bet] > 0 %>
              <li>
                <a>Bet:<strong class="text-info">  <%= session[:bet] %></strong></a>
              </li>
            <% end %>
            <% if session.has_key?(:username) %>
              <li>
                <a><i class="icon-user icon-white"></i> <%= session[:username] %></a>
              </li>
              <li ><a href="/restart"><span class="badge badge-warning">Restart</span></a></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <%if @error %>
        <div class="alert alert-error"><%=@error%></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
  <script src="http://code.jquery.com/jquery.js"></script>
  <script src="/vendor/bootstrap/js/bootstrap.min.js"></script>
</html>
http://blooming-waters-1264.herokuapp.com/